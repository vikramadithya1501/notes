- name: nginx install and run
  hosts: web
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present

- name: run nginx
  hosts: web
  tasks:
  - name: run nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes    